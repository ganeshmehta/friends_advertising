# 3D Models

Drop the plane model here as **`plane.glb`** (preferred) or `plane.gltf`.

The home page orbits it around the Earth via `src/components/OrbitingPlane.tsx`.
If the file is missing the scene falls back to a simple stylized aircraft shape so
nothing breaks.

After dropping the file, just refresh — no rebuild needed in dev.
